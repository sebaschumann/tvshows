<%= form_with(model: season) do |form| %>
  <% if season.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(season.errors.count, "error") %> prohibited this season from being saved:</h2>

      <ul>
        <% season.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number %>
  </div>

  <div class="field">
    <%= form.label :begins_at %>
    <%= form.datetime_select :begins_at %>
  </div>

  <div class="field">
    <%= form.label :novela_id %>
    <%= form.collection_select(:novela_id, Novela.all, :id, :name)  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
